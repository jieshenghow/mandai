import { Workspace } from "@/components/workspace";
import { CartScreen } from "@/components/cart-screen";

export default function Page() {
    return (
        <Workspace section="Cart">
            <CartScreen />
        </Workspace>
    );
}
